import 'package:flutter/material.dart';
class CharModel extends StatelessWidget {
  const CharModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
